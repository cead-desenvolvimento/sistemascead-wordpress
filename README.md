# O que é isso?
Modificações no site www.cead.ufjf.br para consumir o _backend_ do sistema. Os arquivos deste projeto devem ser substituídos em `wp_content/themes/site-cead-novo/`

# Observações
Em `call_backend` do arquivo `page-templates/dao.php` há `$base_url` a ser chamada, verificar se há necessidade de mudança.
